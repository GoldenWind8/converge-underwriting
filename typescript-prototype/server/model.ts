/**
 * The model, in one obvious place. Both providers read it from here, so
 * swapping models is a one-line change.
 */

export const MODEL = "claude-sonnet-5";

/** Reasoning depth. Kept identical across providers so proposals are comparable. */
export const EFFORT = "medium";
